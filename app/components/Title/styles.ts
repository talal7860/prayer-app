import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  title: {
    display: 'flex',
    alignContent: 'center',
    textAlign: 'center',
    fontSize: 20,
    fontWeight: 'bold',
    marginTop: 20,
  },
});
